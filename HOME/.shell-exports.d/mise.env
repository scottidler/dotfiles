#!/usr/bin/env bash

# MISE_HOME/PATH (shims) now live in .zshenv so non-interactive shells get them
# too. This just hooks cd-triggered version-switching into interactive prompts.
eval "$(${HOME}/.local/bin/mise activate zsh)"
