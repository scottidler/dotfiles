# Load encrypted secrets from scottidler/keep repo (formerly scottidler/secrets)
# NOTE: considering moving this file to the keep repo itself (HOME/.shell-exports.d/secrets.env)
eval "$(manifest age decrypt ~/repos/scottidler/keep/.secrets)"
