# Load encrypted secrets from scottidler/secrets repo
# NOTE: considering moving this file to the secrets repo itself (HOME/.shell-exports.d/secrets.env)
eval "$(manifest age decrypt ~/repos/scottidler/secrets/.secrets)"
