<!--
The Architect persona is NOT defined here. It lives in
~/.claude/skills/architect/persona.md and is injected per-call by that skill's
script.sh (prepended to the prompt). Keeping it out of this global file means
plain `gemini` invocations stay neutral instead of always role-playing the
read-only Architect. Do not let `/memory` re-add the Architect lines here.
-->

## Gemini Added Memories
