# herdr, every `claude` process, and all their MCP servers live in a single
# vte-spawn scope under this slice. systemd-oomd kills whole cgroups, so if it
# picks this one every session dies at once. Mark it as a last-resort victim.
[Slice]
ManagedOOMPreference=avoid
